using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class ReadByte : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //byte[] m_buff = new byte[1024];
        //MemoryStream memStream = new MemoryStream(m_buff);
        //BinaryReader reader = new BinaryReader(memStream);
        //byte[] readByte = reader.ReadBytes(0);
        //BinaryWriter writer = new BinaryWriter(memStream);
        //writer.Write(readByte);

        //NetworkSystem networkSystem = new NetworkSystem();
        //networkSystem.SendMsg(123456, 235, new byte[0]);

    }

    // Update is called once per frame
    void Update () {
		
	}
}
